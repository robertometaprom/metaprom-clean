/**
 * Advertising Image package purchases may be stored as balances and sold on /planes
 * only when the first-persist consume path is operational.
 */
export const ADVERTISING_ASSET_FULFILLMENT_OPERATIONAL = true;
